import apropos from "../assets/apropos.png";
import Banner from "../components/Banner";

const Apropos = () => {
  return (
    <div>
      <Banner page={apropos} />
    </div>
  );
};

export default Apropos;
